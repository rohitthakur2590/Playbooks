---
- hosts: vyos
  gather_facts: yes
  module_defaults: 
    vyos_facts:
      gather_subset: default
      gather_network_resources:
        - firewall_rules
  tasks: 
   - name: Delete all the firewalls configured.
     vyos_firewall_rules: &deletd
       config:
       state: deleted
